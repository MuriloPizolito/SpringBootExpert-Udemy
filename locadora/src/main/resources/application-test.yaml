spring:
  application:
    name: locadora
    datasource:
      url: jdbc:mem:h2:testdb
      username: sa
      password:
      driverClassName: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format-sql: true
